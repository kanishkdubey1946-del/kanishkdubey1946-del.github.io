'use client';
import { useState } from 'react';
import Link from 'next/link';
import { ArrowUpRight, Check, Copy } from 'lucide-react';
import type { SiteContent } from '@/lib/content';
export function Footer({person}:{person:SiteContent['person']}){const[copied,setCopied]=useState(false);const[error,setError]=useState(false);async function copy(){try{await navigator.clipboard.writeText(person.email);setCopied(true);setError(false);setTimeout(()=>setCopied(false),2500)}catch{setError(true)}}return <footer id="contact" className="contact-section section-pad"><div className="eyebrow"><span className="section-number">05</span> THE NEXT GOOD PROBLEM</div><div className="contact-heading"><h2>Let’s build<br/><em>something useful.</em></h2><a className="contact-arrow" href={`mailto:${person.email}`} aria-label="Email Kanishk"><ArrowUpRight strokeWidth={1}/></a></div><div className="contact-links"><a href={`mailto:${person.email}`}>{person.email}</a><button className="icon-button" onClick={copy} aria-label={copied?'Email copied':'Copy email address'}>{copied?<Check size={17}/>:<Copy size={17}/>}</button><span className="copy-status" role="status">{copied?'Copied':error?'Please use the email link':''}</span><div className="social-links"><a href={person.github} target="_blank" rel="noreferrer">GitHub <ArrowUpRight size={15}/></a><a href={person.linkedin} target="_blank" rel="noreferrer">LinkedIn <ArrowUpRight size={15}/></a></div></div><div className="footer-bottom"><Link href="/" className="footer-name">Kanishk Dubey</Link><span>© {new Date().getFullYear()} · Made with curiosity.</span><a href="#top">Back to top ↑</a></div></footer>}

