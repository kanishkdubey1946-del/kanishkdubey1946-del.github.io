import { site } from '@/lib/content';
import { ArrowUpRight, Download } from 'lucide-react';
export const metadata = { title: 'Contact', description: 'Connect with Kanishk Dubey for development, UI/UX internships, research and collaborations.' };
export default function Contact() { return <main id="main" className="section-pad archive-page"><header className="page-intro"><div className="eyebrow">LET’S CONNECT</div><h1>A good conversation.<br/><em>A new beginning.</em></h1><p>I’m looking for development and UI/UX internship opportunities, and collaborators working on AI systems, connected hardware and useful digital products.</p><div className="case-actions"><a className="pill-button" href={`mailto:${site.person.email}`}>Email me <ArrowUpRight size={18}/></a><a className="text-link" href={site.person.resume} download>Download résumé <Download size={17}/></a></div></header><div className="contact-directory">{[['Email',site.person.email,`mailto:${site.person.email}`],['GitHub','Explore the code',site.person.github],['LinkedIn','Follow the journey',site.person.linkedin]].map(([label,text,url])=><a key={label} href={url} {...(url.startsWith('https:')?{target:'_blank',rel:'noreferrer'}:{})}><span className="eyebrow">{label}</span><h2>{text}</h2><ArrowUpRight size={24}/></a>)}</div></main>; }
